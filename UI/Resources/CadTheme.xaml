<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:LiteCad.UI">

    <!-- Typography -->
    <Style x:Key="CadSectionLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextSecondaryBrush}" />
        <Setter Property="Margin" Value="0,0,0,4" />
    </Style>

    <Style x:Key="CadPropertyValueStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="CadPanelHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
    </Style>

    <!-- Tooltips -->
    <Style x:Key="CadToolTipStyle" TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource CadToolTipBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
    </Style>

    <!-- Tool button -->
    <Style x:Key="CadToolButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Margin" Value="1,0" />
        <Setter Property="Background" Value="{DynamicResource CadToolButtonBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadToolButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="ToolTipService.ShowDuration" Value="60000" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CadToolButtonPressedBrush}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CadToolButtonPressedBorderBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="{x:Static ui:ToolBarProperties.IsActiveProperty}" Value="False" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource CadToolButtonHoverBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
            </MultiTrigger>
            <Trigger Property="{x:Static ui:ToolBarProperties.IsActiveProperty}" Value="True">
                <Setter Property="Background" Value="{DynamicResource CadToolButtonPressedBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource CadToolButtonPressedBorderBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CadToolIconPathStyle" TargetType="Path">
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="Stroke" Value="{DynamicResource CadIconBrush}" />
        <Setter Property="StrokeThickness" Value="1.2" />
        <Setter Property="StrokeLineJoin" Value="Round" />
        <Setter Property="StrokeStartLineCap" Value="Round" />
        <Setter Property="StrokeEndLineCap" Value="Round" />
        <Setter Property="Fill" Value="Transparent" />
    </Style>

    <Style x:Key="CadToolIconFillPathStyle" TargetType="Path" BasedOn="{StaticResource CadToolIconPathStyle}">
        <Setter Property="Fill" Value="{DynamicResource CadIconBrush}" />
        <Setter Property="StrokeThickness" Value="1" />
    </Style>

    <!-- ComboBox -->
    <Style x:Key="CadComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Height" Value="26" />
        <Setter Property="Padding" Value="6,2" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Background" Value="{DynamicResource CadInputBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
    </Style>

    <!-- StatusBar unit ComboBox toggle (closed chrome only; Popup stays on default SystemColors) -->
    <Style x:Key="CadStatusBarUnitComboToggleStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Background="Transparent">
                        <Path Data="M 0 0 L 4 4 L 8 0 Z"
                              Fill="{DynamicResource CadTextPrimaryBrush}"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Margin="0,0,6,0"
                              Stretch="Uniform"
                              Width="8"
                              Height="5" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- StatusBar mm/m ComboBox: themed closed state; dropdown uses existing SystemColors popup -->
    <Style x:Key="CadStatusBarUnitComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Height" Value="26" />
        <Setter Property="Padding" Value="6,2,20,2" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Background" Value="{DynamicResource CadInputBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid SnapsToDevicePixels="True">
                        <ToggleButton x:Name="ToggleButton"
                                      Style="{StaticResource CadStatusBarUnitComboToggleStyle}"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      ClickMode="Press"
                                      Focusable="False" />
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                IsHitTestVisible="False" />
                        <ContentPresenter x:Name="ContentSite"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          IsHitTestVisible="False"
                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                                    BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                    BorderThickness="1"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CadInputDisabledBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource CadTextMutedBrush}" />
                            <Setter Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CadHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CadAccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource CadAccentBrush}" />
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CadInputBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox -->
    <Style x:Key="CadTextBoxStyle" TargetType="TextBox">
        <Setter Property="Height" Value="26" />
        <Setter Property="Padding" Value="6,2" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Background" Value="{DynamicResource CadInputBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource CadTextPrimaryBrush}" />
    </Style>

    <!-- Inline CAD input field (StatusBar) -->
    <Style x:Key="CadInlineInputBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CadInputDisabledBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="CadInlineInputTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="5,0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="CaretBrush" Value="{DynamicResource CadTextPrimaryBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
    </Style>

    <Style x:Key="CadInlineInputDisplayStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="6,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextSecondaryBrush}" />
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

    <!-- CheckBox -->
    <Style x:Key="CadCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource CadTextPrimaryBrush}" />
        <Setter Property="Margin" Value="0,0,0,6" />
    </Style>

    <!-- Separator -->
    <Style x:Key="CadToolbarSeparatorStyle" TargetType="Separator">
        <Setter Property="Width" Value="1" />
        <Setter Property="Margin" Value="6,4" />
        <Setter Property="Background" Value="{DynamicResource CadBorderBrush}" />
    </Style>

    <Style x:Key="CadStatusSeparatorStyle" TargetType="Separator">
        <Setter Property="Width" Value="1" />
        <Setter Property="Height" Value="18" />
        <Setter Property="Margin" Value="12,0" />
        <Setter Property="Background" Value="{DynamicResource CadBorderBrush}" />
    </Style>

    <!-- Panel chrome -->
    <Style x:Key="CadToolbarPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CadPanelBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>

    <Style x:Key="CadStatusBarPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CadBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <Setter Property="Padding" Value="8,3" />
    </Style>

    <Style x:Key="CadPropertiesPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CadPropertiesBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="BorderThickness" Value="1,0,0,0" />
    </Style>

    <Style x:Key="CadMenuBarPanelStyle" TargetType="Grid">
        <Setter Property="Background" Value="{DynamicResource CadBackgroundBrush}" />
    </Style>

    <!-- Property section -->
    <Style x:Key="CadPropertySectionStyle" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>

    <Style x:Key="CadPropertyFieldStyle" TargetType="StackPanel">
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <!-- Menu -->
    <Style x:Key="CadMenuStyle" TargetType="Menu">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource CadMenuForegroundBrush}" />
    </Style>

    <Style x:Key="CadMenuSeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource CadBorderBrush}" />
        <Setter Property="Margin" Value="0,2" />
    </Style>

    <Style x:Key="CadMenuItemStyle" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource CadMenuForegroundBrush}" />
        <Setter Property="ItemContainerStyle" Value="{DynamicResource CadMenuItemStyle}" />
        <Style.Resources>
            <Style TargetType="Separator" BasedOn="{StaticResource CadMenuSeparatorStyle}" />
        </Style.Resources>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource CadMenuHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource CadMenuForegroundBrush}" />
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter Property="Background" Value="{DynamicResource CadMenuHoverBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource CadMenuForegroundBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource CadMenuDisabledForegroundBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
